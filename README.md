# Sentinel
log analysis platform that ingests traffic events via a Python API, queues them in Redis to prevent blocking, processes them for security threats, and streams the results to a React dashboard via WebSockets for sub-second live updates
